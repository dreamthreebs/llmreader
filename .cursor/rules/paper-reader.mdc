---
description: llmreader 项目的论文阅读目录约定。在此项目中阅读论文时自动启用，配合通用 paper-reader skill 使用。
alwaysApply: false
---

# llmreader 项目约定

本项目使用通用 paper-reader skill 进行论文阅读。以下是本项目特有的目录和文件约定（通用 skill 中的启动询问可跳过）。

## 输入源

本项目以 **LaTeX 源码**为主要输入。论文源码已存放在 `papers/<id>/` 中。

## 目录结构

```
llmreader/
├── papers/<id>/          # 原始文献（LaTeX 源码优先，PDF 备选）
│   ├── main.tex
│   └── figures/
├── readings/<id>/        # 阅读笔记（每篇一个文件夹）
│   ├── §1.md, §2.md, …  # 每 section 一个精读笔记
│   ├── 总纲.md
│   ├── 故事版.md
│   └── 图表版.md
└── index.md              # 文献索引：arXiv ID / 标题 / 主题 / 阅读日期
```

## 图片处理

- `papers/<id>/figures/` 中的 PDF 图片转为 PNG（存在原目录）
- 图表版用相对路径 `../../papers/<id>/figures/figX.png` 引用
- 图片不进 git（`papers/` 在 `.gitignore` 中），仓库只存文本
